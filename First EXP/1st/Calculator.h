#ifndef CALCULATOR_H
#define CALCULATOR_H
#include <math.h>
#define ADD(x, y) ((x) + (y))
#define SUB(x, y) ((x) - (y))
#define MUL(x, y) ((x) * (y))
#define DIV(x, y) ((x) / (y))
#define POWER_OF_2(x) (pow((x), 2))
#define SQUARE_ROOT(x) (sqrt(x))
#define CUBE_ROOT(x) (pow(x, 1.0/3.0))
#endif